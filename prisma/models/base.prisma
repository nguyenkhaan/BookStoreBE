enum BookCategory {  //check 
    VAN_HOC 
    TRINH_THAM 
    THIEU_NHI 
    GIAO_DUC 
    KINH_TE 
    KY_NANG_SONG 
}

enum Role {   //check 
    ADMIN
    EMPLOYEE
    CUSTOMER
}
enum MemberGrade {  //check 
    BRONZE 
    GOLD 
    SILVER 
    DIAMOND 
}
enum RuleType {
    HUMAN 
    SALE 
    LOGISTIC 
    SERVICE 
    FINANCE 
    SAFETY  
}
enum VoucherType {
    VND 
    PERCENT 
}
enum IncomeStatus {  //check 
    COMPLETE
    PENDING
    CANCEL 
}

enum OutcomeStatus {
    COMPLETE
    PENDING
    CANCEL 
}

enum VoucherStatus {
    APPLYING
    UPCOMING 
    ENDED
}

enum TokenType {
    RESET_PASSWORD
    ACCESS
    REFRESH
    VERIFY_EMAIL
}

enum EmployeeStatus {
    WORKING
    LEAVE //Nghi phep 
    RETIRED
}
enum RuleStatus {
    APPLYING 
    REJECT 
    
}
enum BillStatus {  //Check 
    COMPLETE 
    NOT_STARTED 
    OVERDUE  
} 
enum IncomePaymentType {  //Check 
    CASH 
    TRANSFER 
    CARD 
}