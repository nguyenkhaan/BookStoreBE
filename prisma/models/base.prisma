enum BookCategory {
    HORROR
    MANGA
    SCIENCE_FICTION
}

enum Role {
    ADMIN
    EMPLOYEE
    CUSTOMER
}

enum IncomeStatus {
    NOT_STARTED
    COMPLETE
    PENDING
    FAILED
}

enum OutcomeStatus {
    NOT_STARTED
    COMPLETE
    PENDING
    FAILED
}

enum VoucherStatus {
    APPLYING
    ENDED
}

enum TokenType {
    RESET_PASSWORD
    ACCESS
    REFRESH
    VERIFY_EMAIL
}

enum EmployeeStatus {
    WORKING
    RETIRED
}
