enum BookCategory {
    HORROR
    MANGA
    SCIENCE_FICTION
}

enum Role {
    ADMIN
    EMPLOYEE
    CUSTOMER
}
enum MemberGrade {
    BRONZE 
    GOLD 
    SILVER 
    DIAMOND 
}
enum RuleType {
    HUMAN 
    WORKING 
    ADMINISTRATIVE  
}
enum VoucherType {
    VND 
    PERCENT 
}
enum IncomeStatus {
    NOT_STARTED
    COMPLETE
    PENDING
    FAILED
}

enum OutcomeStatus {
    NOT_STARTED
    COMPLETE
    PENDING
    FAILED
}

enum VoucherStatus {
    APPLYING
    ENDED
}

enum TokenType {
    RESET_PASSWORD
    ACCESS
    REFRESH
    VERIFY_EMAIL
}

enum EmployeeStatus {
    WORKING
    RETIRED
}
enum RuleStatus {
    APPLYING 
    REJECT 
    
}
enum BillStatus {
    COMPLETE 
    PENDING 
    
}