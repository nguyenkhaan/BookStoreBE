model Customer {
    id        Int          @id @default(autoincrement())
    code      String?      @unique
    name      String
    email     String?      @unique
    phone     String       @unique
    password  String?
    createdAt DateTime     @default(now())
    updatedAt DateTime     @updatedAt
    bill      Bill[]
    deletedAt DateTime?
    grade     MemberGrade?
}
